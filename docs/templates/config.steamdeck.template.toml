# Steam Deck template (SteamOS + Flatpak focused).
# SteamOS root is immutable; keep mutable GAMEHUB state under /home/deck.
[server]
url = "http://<SERVER_IP>:8000"

# Optional index fetch hardening for slower/unreliable links.
# index_timeout_seconds = 45
# index_fetch_attempts = 3
# index_retry_backoff_seconds = 1.5
# Parallel download workers for firmware/ROM/assets (1-16, default 4).
# max_parallel_downloads = 4

[paths]
gamehub_dir = "/home/deck/GameHub"
# Optional: store ROM output on microSD while keeping firmware/state on internal storage.
# Typical Steam Deck mount format: /run/media/deck/<SD_CARD_LABEL>/GameHub/roms
# roms_dir = "/run/media/deck/<SD_CARD_LABEL>/GameHub/roms"

[steam]
# Steam Deck Desktop + Game Mode commonly use this userdata root.
userdata_dir = "/home/deck/.local/share/Steam/userdata"
# Alternate Steam roots seen on some SteamOS installs.
# userdata_dir = "/home/deck/.steam/root/userdata"
# userdata_dir = "/home/deck/.steam/steam/userdata"
# Optional but recommended for deterministic profile targeting.
# steam_id = "76561198000000001"

[linux]
# SteamOS should prefer Flatpak for emulator lifecycle/config parity.
emulator_install_backend = "flatpak"
flatpak_remote = "flathub"

# Optional Dolphin runtime user dir examples:
# Native:
# dolphin_user_path = "~/.local/share/dolphin-emu"
# Flatpak:
# dolphin_user_path = "~/.var/app/org.DolphinEmu.dolphin-emu/data/dolphin-emu"

[sgdb]
# Fallback only; prefer GAMEHUB_SGDB_API_KEY in environment.
# api_key = "<OPTIONAL_SGDB_KEY>"
cache_dir = "/home/deck/GameHub/artwork-cache/sgdb"
enabled_kinds = ["grid", "hero", "logo", "icon"]

[controllers]
# Steam Deck default should favor the built-in controller in launch autoconfig.
launch_autoconfig = true
